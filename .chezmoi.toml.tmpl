[data]
    name = "name"
    email = "user@example.com"
    usage = "private" # (private,work)
    keyboard = "default" # (default,advantage360)
    gpgsignkey = "keyid"
    bwSessionPath = "path"
    bwEmail = "user@example.com"
    anthropic_api_key = "key"
    openai_api_key = "key"
    aider_model = ""

[diff]
    pager = "delta"
    command = "delta"
    args = ["{{ `{{ .Destination }}` }}", "{{ `{{ .Target }}` }}"]
[merge]
    command = "nvim"
    args = ["-d", "{{ `{{ .Destination }}` }}", "{{ `{{ .Source }}` }}", "{{ `{{ .Target }}` }}"]
